<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
</head>
<body>
    <div>
        <ul>
            {{#each files}}
            <li>{{this}}</li>
            {{/each}}
        </ul>
    </div>
</body>
</html>